name: Release

on:
  workflow_dispatch:
    inputs:
      bump:
        description: "バージョンの種類"
        required: true
        type: choice
        options:
          - "バグフィックス"
          - "マイナーバージョンアップ"
          - "メジャーバージョンアップ"

permissions:
  contents: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    runs-on: windows-latest
    steps:
      - name: Verify CI passed
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $conclusion = gh api repos/${{ github.repository }}/commits/${{ github.sha }}/check-suites `
            --jq '.check_suites[] | select(.app.slug == "github-actions") | .conclusion' `
            | Select-Object -First 1
          if ($conclusion -ne "success") {
            Write-Error "CI has not passed on this commit (status: $conclusion). Push and wait for CI to pass before releasing."
            exit 1
          }

      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup tools
        uses: jdx/mise-action@v3

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2

      - name: Configure git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Bump version
        id: bump
        shell: pwsh
        run: |
          $content = Get-Content Cargo.toml -Raw
          if ($content -match '(?m)^version = "(\d+)\.(\d+)\.(\d+)"') {
            $major = [int]$Matches[1]
            $minor = [int]$Matches[2]
            $patch = [int]$Matches[3]
          } else {
            throw "version not found in Cargo.toml"
          }

          $bump = "${{ inputs.bump }}"
          switch ($bump) {
            'バグフィックス'             { $patch++ }
            'マイナーバージョンアップ'   { $minor++; $patch = 0 }
            'メジャーバージョンアップ'   { $major++; $minor = 0; $patch = 0 }
          }

          $newVersion = "$major.$minor.$patch"
          $content = $content -replace '(?m)^version = "\d+\.\d+\.\d+"', "version = `"$newVersion`""
          Set-Content Cargo.toml -Value $content -NoNewline

          Write-Output "version=$newVersion" >> $env:GITHUB_OUTPUT
          Write-Host "New version: $newVersion"

      - name: Update Cargo.lock
        run: cargo generate-lockfile

      - name: Build release
        run: mise run build-release

      - name: Create release archive
        shell: pwsh
        run: |
          $version = "${{ steps.bump.outputs.version }}"
          $zipName = "gv3-v$version.zip"
          Compress-Archive -Path @(
            "target/release/gv3.exe",
            "gv3.default.toml",
            "gv3.keys.default.toml",
            "README.md",
            "LICENSE"
          ) -DestinationPath $zipName

      - name: Commit, tag and push
        run: |
          git add Cargo.toml Cargo.lock
          git commit -m "release: v${{ steps.bump.outputs.version }}"
          git tag "v${{ steps.bump.outputs.version }}"
          git push origin HEAD --tags

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "v${{ steps.bump.outputs.version }}" `
            "gv3-v${{ steps.bump.outputs.version }}.zip" `
            --title "v${{ steps.bump.outputs.version }}" `
            --generate-notes
